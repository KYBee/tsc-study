# 저장소 작업 지침

이 파일은 저장소 전체에 적용된다. 하위 디렉터리에 더 구체적인 `AGENTS.md`가 생기면 해당 범위에서는 하위 지침을 함께 따른다.

## 작업 전 확인

1. 모든 작업을 시작하기 전에 `README.md`와 `docs/INDEX.md`를 읽는다.
2. 작업 주제와 관련된 문서를 확인하고 확정 요구사항, 미결정 사항, 데이터 규칙을 구분한다.
3. 코드 작성이나 데이터 변환 전에 `docs/DATA_WORKFLOW.md`, `docs/DATA_SCHEMA.md`, `docs/DECISIONS.md`를 확인한다.
4. 중국어 답변 교정과 관련된 작업은 `docs/AI_CORRECTION_RULES.md`를 먼저 확인한다.
5. 결정되지 않은 기술 사항을 임의로 확정하지 않는다.
6. 실제 앱 개발 전에는 `docs/IMPLEMENTATION_BASELINE.md`와 관련 UI·화면 데이터 계약을 확인한다.

## 제품 원칙

- 이 프로젝트의 우선 목표는 새로운 고급 표현을 늘리는 것이 아니라 중국어 답변의 실수를 줄이고, 파트별 답변 구조와 필수 표현을 익히게 하는 것이다.
- Level 8 목표가 있더라도 기본 동작은 사용자의 표현을 살리는 최소 교정이다.
- 녹음, 음성 인식, 음성 평가, 질문 음성 재생은 현재 우선순위에서 제외한다.
- 요청받지 않은 기능 추가, 대규모 리팩터링, 기술 스택 도입을 하지 않는다.

## 데이터 원칙

- 질문 데이터와 모범답안 데이터의 분리를 유지한다.
- 답변이 없는 질문은 정상 상태다. 이를 누락 오류나 불완전한 데이터로 처리하지 않는다.
- 하나의 질문에 기본 답변과 Level 8 확장 답변 등 여러 답변이 연결될 수 있어야 한다.
- 공용 문제 데이터와 사용자의 개인 학습 기록을 분리한다.
- `Correction`은 오류 내용과 수정 이유·출처만 관리한다. 사용자별 `못 외움`, `헷갈림`, `외움` 상태는 `ReviewState`에서만 관리한다.
- `Question`의 고유 식별자는 `question_id`이며 `question_zh`를 고유키로 사용하지 않는다.
- 시각 문제와 `Question`의 연결은 명확한 근거가 있을 때만 만들고 추측하지 않는다.
- 출처 관계는 `SourceReference`로 기록하며 `SourceReference` 없이 출처를 확정하지 않는다.
- `StoryGuide`는 이야기 구성 보조 콘텐츠이며 `ModelAnswer`로 변환하지 않는다.
- 원본 자료는 `raw`, 정리 중 자료는 `working`, 검수 완료 자료는 `reviewed` 단계로 관리한다. `raw` 원본의 내용과 원래 파일명은 변경하지 않는다.
- 원본 자료 단계에서는 병음이나 한국어 뜻이 비어 있을 수 있다. 검수 완료 데이터와 사이트 표시 데이터의 중국어 문제·표현·답변은 중국어 원문, 병음, 한국어 뜻을 함께 관리한다.
- 실제 출처에서 확인한 데이터, 출처 미확인 데이터, 자체 생성한 변형 데이터를 명확히 구분한다.
- 실제 강의 내용과 임의 생성 내용을 섞지 않으며, 출처를 모르는 내용을 확정된 사실처럼 작성하지 않는다.
- 공용 reviewed 데이터의 canonical 형식은 엔터티별 JSON이다.
- `data/working` CSV를 서비스 기준 데이터처럼 직접 수정하거나 런타임에서 사용하지 않는다.
- 개인 `UserAnswer`, 개인 `Correction`, `ReviewState`를 공용 reviewed JSON에 넣지 않는다.

## 중국어 교정 원칙

- 사용자의 중국어에는 문법, 어순, 단어 선택, 내용 연결 오류가 있을 수 있다고 가정한다.
- 사용자가 쓴 단어, 문장 구조, 의미, 개인 경험을 최대한 유지하고 틀리거나 매우 부자연스러운 부분만 먼저 고친다.
- 사용자가 말하지 않은 개인 정보나 경험을 만들지 않는다.
- 질문과 관련 없는 내용은 이유를 설명하고 바로잡는다.
- 기본 최소 교정과 별도의 “Level 8답게 확장” 결과를 섞지 않는다.
- 교정 결과에는 중국어, 전체 병음, 한국어 뜻, 수정 전후 표현, 수정 이유, 답변 구조를 포함한다.
- 확신할 수 없는 내용은 단정하지 말고 확인 필요 상태로 남긴다.

## 작업 규율

- 코드 작성 전에 관련 문서와 데이터 규칙을 먼저 확인한다.
- 요청 범위 안에서 가장 작은 변경을 한다.
- 초기 앱 구현은 `docs/VERTICAL_SLICE_PLAN.md`의 Part 4 수직 기능부터 시작한다.
- 실제 AI API는 별도 승인 전 연결하지 않으며 클라이언트 코드에 API 키를 넣지 않는다.
- 기존 원본 파일을 임의로 덮어쓰거나 이름을 변경하거나 정규화하지 않는다. 새 파일명은 `working` 또는 `reviewed` 파생본에만 사용하고 `original_file_name`으로 원래 이름을 추적한다.
- `docs/IMPLEMENTATION_BASELINE.md`와 `docs/DECISIONS.md`에서 확정한 MVP 기준을 따르고, 패키지 버전·AI 제공자·백엔드 기술 등 남은 미결정 사항을 편의상 고정하지 않는다.
- 작업 후 변경 파일, 새로 내린 결정, 미해결 사항, 검증 결과를 요약한다.
