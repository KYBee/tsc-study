# TSC 분석 처리 로그

## 2026-07-18

- 작업 시작. `challenge.md` 932줄 전체 요구사항을 확인했다.
- 저장소 상태 확인 결과 `/Users/kybee/dev/china`는 Git 저장소가 아니다.
- 원본 폴더 `lecture1/`~`lecture7/`과 MP4 13개, PDF 3개, DOCX 1개를 1차 확인했다. 확정 수량과 중복 여부는 기술 검사 후 인벤토리에 기록한다.
- `/opt/homebrew/bin` 기준 Poppler(`pdfinfo`, `pdftotext`, `pdftoppm`)는 사용 가능하다.
- PATH에서 `ffmpeg`, `ffprobe`, Whisper 계열 명령을 찾지 못했다. 인터넷은 사용하지 않고 로컬 대체 수단과 기존 설치물을 조사한다.
- PDF는 전 페이지 렌더링과 텍스트 대조, DOCX는 OOXML 추출 및 가능한 경우 전 페이지 렌더링으로 검증한다.
- AVFoundation으로 MP4 13개의 메타데이터를 읽고 영상ㆍ음성 압축 샘플을 끝까지 순회했다. 전 파일이 재생 가능하고 두 트랙 모두 `completed`였다.
- 파일 기준 총재생시간은 09:22:39이다. SHA-256으로 04강 내부 중복 1쌍과 06강/07강 간 중복 1쌍을 확인했으며, 중복 제거 고유 콘텐츠 11개의 총재생시간은 07:59:20이다.
- macOS 26 `SpeechTranscriber`의 설치 모델을 조사했다. 한국어 `ko_KR`만 설치되어 있고 중국어 모델은 미설치다. 인터넷 금지 원칙에 따라 모델 다운로드는 시도하지 않는다.
- 한국어 온디바이스 전사 시험에 성공했다. AAC를 16kHz mono PCM으로 변환해야 하며, 단어별 타임스탬프가 생성된다. 중국어 발화는 한국어 모델 결과를 신뢰하지 않고 화면 및 문서 대조 대상으로 표시한다.
- 전사 일괄 파이프라인 첫 실행은 01ㆍ02강의 깨진 원본 파일명이 메타데이터 JSON과 파일시스템에서 서로 다른 유니코드 정규화 형태로 표현되어 경로 매핑에 실패했다. 원본이나 산출물 손상은 없으며, 경로 비교를 NFC로 정규화하도록 수정했다.
- 장면 변화 추출 1차 실행에서 360pㆍ720p 고유 영상 6개는 성공했으나 1080p 영상 5개가 AVFoundation의 320×180 직접 축소 디코딩 설정을 거부했다(`AVFoundationErrorDomain -11832`). 원본 손상이 아니라 출력 설정 호환 문제이므로 1080p는 네이티브 해상도로 디코딩하고 장면 서명 계산만 축소하는 방식으로 재처리한다.
- 네이티브 디코딩 뒤 Core Image로 장면 이미지를 저장하도록 변경해 고유 영상 11개 전부의 순차 프레임 분석을 완료했다. 장면 메타데이터 기준 749개를 선별했고, 중간 실패 시도에서 남은 이미지를 포함해 `output/screenshots/`에는 PNG 766개가 있다. 모든 화면 인덱스 링크는 검증 시 정상이다.
- 로컬 Vision OCR은 중국어 간체, 한국어, 영어 자동 감지를 사용했다. 초기 한국어 우선 언어 순서의 중국어 인식이 낮아 중국어 우선으로 재실행했으며, 장면 749개에서 처리 실패는 0개다.
- PDF 3개 261페이지의 텍스트를 페이지별로 추출하고 1600×900 PNG로 렌더했다. 18개 접촉시트를 `view_image`로 확인해 모든 페이지를 시각 검토했다.
- DOCX용 패키지 렌더 스크립트는 환경에 `pdf2image`가 없어 실행 전 실패했고, 인앱 브라우저도 연결된 탭이 없어 전체 렌더 대안을 사용할 수 없었다. 대신 OOXML 문단ㆍ직접 서식ㆍ이미지 유무를 전부 추출하고 macOS Quick Look 첫 화면을 확인했다. Word 메타데이터는 3페이지, 명시적 렌더 페이지 나눔은 0개다.
- 온디바이스 한국어 전사는 13개 MP4 모두 생성했다. 완전 중복 2개는 대표본 전사를 연결했다. 고유 콘텐츠 기준 신뢰도 0.55 미만 토큰은 2,014개, 5초 이상 무인식 후보는 480구간이며 각 전사에 그대로 표기했다.
- 01~07강 상세분석을 각 18개 필수 절로 작성하고, 통합분석 13절과 학습 자료 6종을 생성했다. 중국어 표현은 화면ㆍPDFㆍDOCX에서 식별된 표기만 최종 자료에 채택했다.
- 최종 검증은 MP4 13/manifest 13/전사 13/화면 인덱스 13, PDF 페이지 261/261, 강의별 18절 7/7, 통합 13절, 학습 자료 6/6, 빈 Markdown 0, 깨진 로컬 링크 0, 표 열 불일치 0을 확인했다. 최대 전사 말미 차이는 22초이며 종료 전 무음 후보로 보존했다.
